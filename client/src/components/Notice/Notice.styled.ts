import styled from "@emotion/styled";

export const NoticeContainer = styled.div`
  position: fixed;
  top: auto;
  left: 37%;
  right: auto;
  bottom: 20px;
  z-index: 1051;
  & > div {
    padding: 4px 0 4px 0;
  }
`;
